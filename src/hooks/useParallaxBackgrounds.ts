/**
 * Re-export useScrollParallax and lerp under useParallaxBackgrounds
 * for consistent API access across components.
 */
export { useScrollParallax, useParallaxBackgrounds, lerp, isReducedMotionActive } from './useScrollParallax';
